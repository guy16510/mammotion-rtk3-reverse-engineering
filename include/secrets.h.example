#pragma once

// Optional. You can instead configure Wi-Fi from the fallback access point at
// http://192.168.4.1/ and the ESP32 will save the credentials in NVS.
#define WIFI_SSID "your-home-wifi"
#define WIFI_PASSWORD "your-password"
