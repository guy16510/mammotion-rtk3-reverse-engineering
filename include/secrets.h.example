#pragma once

// Copy this file to include/secrets.h. That file is gitignored.
// Leave these empty to use only the RTK3-Probe fallback access point.
#define WIFI_SSID "your-home-wifi"
#define WIFI_PASSWORD "your-wifi-password"
