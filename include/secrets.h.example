#pragma once

// Optional local Wi-Fi credentials. Copy this file to include/secrets.h only if
// you do not want to configure Wi-Fi through the RTK3-Probe fallback access point.
// include/secrets.h is gitignored and must never be committed.
#define WIFI_SSID "your-home-wifi"
#define WIFI_PASSWORD "your-password"

// Non-secret device target defaults are committed in config/rtk3-target.json.
#define RTK3_IP "192.168.2.26"
#define RTK3_LORA_ID "915.24.0.35592"
